### Clay's 2020 Solutions (Go)

1. Download (Go)[https://golang.org/doc/install]
2. Add a valid input.txt file to this directory
3. `go run sol.go` to skip compilation OR `go build` to generate a binary
4. ./dayX to run it!
